/*
 * This file was generated by the Gradle 'init' task.
 *
 * This is a general purpose Gradle build.
 * Learn how to create Gradle builds at https://guides.gradle.org/creating-new-gradle-builds/
 */

task makeDataSet(type: Exec) {
    workingDir "$projectDir/src/data/"
    if (System.getProperty('os.name').toLowerCase(Locale.ROOT).contains('windows')) {
        commandLine 'cmd', '/c', 'mycommand'
    } else {
        commandLine 'sh', 'wav2features.sh'
    }
}

task trainModel(type: Exec) {
    workingDir "$projectDir/src/models/"
    if (System.getProperty('os.name').toLowerCase(Locale.ROOT).contains('windows')) {
        commandLine 'cmd', '/c', 'mycommand'
    } else {
        commandLine 'python', 'train_model.py'
    }
}

task makeReportSvmBasic(type: Exec) {
    workingDir "$projectDir/src/models/"
    if (System.getProperty('os.name').toLowerCase(Locale.ROOT).contains('windows')) {
        commandLine 'cmd', '/c', 'mycommand'
    } else {
        commandLine 'python', 'make_report.py', 'svm_basic'
    }
}

task makeReportGMMBasic(type: Exec) {
    workingDir "$projectDir/src/models/"
    if (System.getProperty('os.name').toLowerCase(Locale.ROOT).contains('windows')) {
        commandLine 'cmd', '/c', 'mycommand'
    } else {
        commandLine 'python', 'make_report.py', 'GMM_basic'
    }
}